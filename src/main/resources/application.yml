spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dpls_db
    username: postgres
    password: plutoski12
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: my-very-long-secret-key-that-is-at-least-256-bits-long
  expiration: 86400000